name: Check backport

on:
  pull_request_target:

permissions:
  contents: read

jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - name: Prepare parameters
        run: |
          echo "${{ toJSON(github.event.pull_request) }}"

# TEST